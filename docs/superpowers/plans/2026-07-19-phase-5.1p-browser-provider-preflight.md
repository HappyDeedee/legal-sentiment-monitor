# Phase 5.1P Browser Provider Preflight Packet

> Completed and independently reviewed on 2026-07-19 as the strictly read-only
> CR-047 preflight. Its verified output is
> `docs/phase-5.1p-browser-entrypoint-map.md`; Phase 5.1A is now the next
> eligible execution unit.

**Goal:** Map every browser/login/crawl entrypoint and define the minimum
`BrowserEnvironmentProvider` contract that Phase 5.1A-D can implement without
guessing.

**Must not change:** code, schema, UI, runtime data, databases, Profiles,
Cookies, proxies, browser processes, crawler behavior, deployment, or CR-112
implementation status.

## Inputs

- `docs/TASKS.md` Phase 5.1P
- `docs/ACCOUNT_ENVIRONMENT.md`
- `docs/SERVER_DEPLOYMENT.md`
- `api/monitoring/login_browser.py`
- `api/monitoring/login_qrcode.py`
- `api/monitoring/account_check.py`
- `api/monitoring/runner.py`
- `api/services/crawler_manager.py`
- `tools/browser_launcher.py`
- `tools/cdp_browser.py`
- MediaCrawler launch/reconnect callers found from these entrypoints

## Tasks

- [x] Confirm the current-main/worktree baseline and classify overlapping open
      tasks as current, gated, future-only, deferred, or historical.
- [x] Create `docs/phase-5.1p-browser-entrypoint-map.md` with one row per QR
      login, visible browser login, Cookie validation, Profile login-state
      check, manual run, scheduler run, runner launch, CDP launch, and CDP
      reconnect path.
- [x] For every row record caller, executable source, Profile source, proxy
      source, user-agent/locale/timezone/viewport source, headless mode,
      Playwright/CDP ownership, fallback behavior, validation, locks,
      finalization, and requested/effective evidence.
- [x] Trace `MONITOR_BROWSER_EXECUTABLE` from configuration to actual consumers
      and record every path that bypasses it.
- [x] Trace Cookie login end to end: account save, temporary validation,
      persistent Profile creation/reuse, Cookie injection, runner argv/env,
      CDP launch, standard-mode fallback, browser close, and restart. Mark
      exactly where current behavior persists Profile state and where it uses
      an ephemeral or shared fallback Profile.
- [x] Record that current MediaCrawler exposes only `qrcode`, `phone`, and
      `cookie` login branches and identify the exact caller/adapter changes a
      future CR-112 internal `profile_only` mode would require to replace
      managed `login_type=cookie` child execution. Keep existing QR/Profile
      execution and that future target separate from current-main evidence and
      do not implement either here.
- [x] Map `cmd_arg/arg.py` Cookie defaults, `main.py` crawler creation/error
      handling, every platform's `pong`-then-login branch, CDP-to-standard
      fallback, `runner.py` return-code/log heuristics, and existing Profile
      environment variables so the future hidden flag/exit-code contract has
      named adapter points rather than a second resolver.
- [x] Define an immutable provider result containing browser path/family/source,
      `profile_key`, derived Profile path, proxy, environment inputs, headless
      and provider mode, requested values, effective values, unsupported fields,
      mismatch evidence, and `fallback_used`.
- [x] Classify each field as required/provable, supported/not-provable,
      unsupported, or diagnostic-only for the current Playwright/CDP provider.
- [x] Prove whether one provider result can serve all mapped paths. Record any
      caller-specific adapter that is still required without creating a second
      authority.
- [x] Define fail-closed behavior for invalid explicit executable, locked
      identity mismatch, unsupported required field, hidden task proxy override,
      process defaults, and default-network fallback.
- [x] Keep local Chrome/Edge detection, visible login, and connect-existing CDP
      as diagnostic fallbacks; server-like execution remains acceptance proof.
- [x] Obtain an independent read-only review of the complete map, callers, and
      missing evidence before Phase 5.1A starts.

## Exit Criteria

- The map has no unclassified entrypoint or hidden fallback.
- One provider contract covers login, validation, checks, runs, and CDP paths.
- Requested/effective snapshot fields and proof methods are explicit.
- The current gap between ephemeral Cookie validation, persistent crawl
  Profile reuse, and any generic-profile fallback is explicit enough for Phase
  5.1D and CR-112 to remove split authority without guessing.
- The map names which Phase 5.1 provider guarantees CR-112 may reuse and which
  internal crawler-mode, promotion-journal, and Cookie-protocol changes remain
  owned by CR-112 rather than silently widening CR-047.
- Unsupported surfaces are named and are not silently claimed.
- Phase 5.1A-D can start without a browser/Profile/proxy authority decision.
- Project documentation checks and whitespace checks pass after the read-only
  evidence document is added.

## Stop Conditions

- A caller cannot consume the provider contract without changing accepted
  Phase 5.1 scope.
- A locked identity would still depend on process defaults or hidden fallback.
- Server-like proof cannot observe a required effective value.
- Current docs and code disagree on the authority for a field and the existing
  decision precedence does not resolve it.

When stopped, record the ambiguity as `Needs Confirmation`; do not start
Phase 5.1A-D or any CookieBridge packet.
